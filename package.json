{
  "name": "dotnet-svelte-template",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack template for ASP.NET Core Minimal API backend and SvelteKit frontend",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cross-env DOTNET_USE_POLLING_FILE_WATCHER=true dotnet watch --non-interactive --project backend/DotnetSvelte.Api",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "dotnet build backend/DotnetSvelte.slnx -c Release",
    "publish:backend": "dotnet publish backend/DotnetSvelte.Api -c Release -o dist/backend",
    "check": "npm run check --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^10.1.0"
  }
}
